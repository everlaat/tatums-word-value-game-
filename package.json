{
  "scripts": {
    "start": "parcel start index.html",
    "build": "rm -rf .cache docs && parcel build index.html --no-minify --out-dir ./docs --public-url ./"
  },
  "devDependencies": {
    "cssnano": "^5.0.6",
    "postcss": "^8.3.5",
    "typescript": "^4.3.5"
  }
}
